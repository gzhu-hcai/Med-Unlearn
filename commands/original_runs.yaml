- isic:
  - original:
    - model_resnet18-unlearner.cfg.batch_size_256-unlearner.cfg.num_epochs_50-unlearner.cfg.optimizer.learning_rate_0.1-unlearner_original
    - model_vit11m-unlearner.cfg.batch_size_256-unlearner.cfg.num_epochs_50-unlearner.cfg.optimizer.learning_rate_0.1-unlearner_original
  - naive :
    - model_resnet18-unlearner.cfg.batch_size_256-unlearner.cfg.num_epochs_50-unlearner.cfg.optimizer.learning_rate_0.1-unlearner_naive
    - model_vit11m-unlearner.cfg.batch_size_256-unlearner.cfg.num_epochs_50-unlearner.cfg.optimizer.learning_rate_0.1-unlearner_naive

- busi:
  - original:
    - model_resnet18-unlearner.cfg.batch_size_256-unlearner.cfg.num_epochs_25-unlearner.cfg.optimizer.learning_rate_0.1-unlearner_original
    - model_vit11m-unlearner.cfg.batch_size_256-unlearner.cfg.num_epochs_25-unlearner.cfg.optimizer.learning_rate_0.1-unlearner_original
  - naive :
    - model_resnet18-unlearner.cfg.batch_size_256-unlearner.cfg.num_epochs_25-unlearner.cfg.optimizer.learning_rate_0.1-unlearner_naive
    - model_vit11m-unlearner.cfg.batch_size_256-unlearner.cfg.num_epochs_25-unlearner.cfg.optimizer.learning_rate_0.1-unlearner_naive

- mri:
  - original:
    - model_resnet18-unlearner.cfg.batch_size_256-unlearner.cfg.num_epochs_30-unlearner.cfg.optimizer.learning_rate_0.1-unlearner_original
    - model_vit11m-unlearner.cfg.batch_size_256-unlearner.cfg.num_epochs_30-unlearner.cfg.optimizer.learning_rate_0.1-unlearner_original
  - naive :
    - model_resnet18-unlearner.cfg.batch_size_256-unlearner.cfg.num_epochs_30-unlearner.cfg.optimizer.learning_rate_0.1-unlearner_naive
    - model_vit11m-unlearner.cfg.batch_size_256-unlearner.cfg.num_epochs_30-unlearner.cfg.optimizer.learning_rate_0.1-unlearner_naive
